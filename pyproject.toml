[project]
name = "letters"
version = "0.1.0"
description = "Crawl and convert shareholder letter archives."
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.12.3",
    "brotli>=1.1.0",
    "requests>=2.32.3",
]

[tool.uv]
package = false
